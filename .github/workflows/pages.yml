name: Deploy Docs to GitHub Pages

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    # 仅在正式版 release 时自动部署（tag 不含 - 预发布后缀），手动触发不受限制。
    if: github.event_name != 'release' || !contains(github.event.release.tag_name, '-')
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # 拉取完整 git 历史，供 Starlight lastUpdated 时间戳使用（浅克隆会导致日期为空）。
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: |
          cd docs
          npm ci

      - name: Build Astro site
        run: |
          cd docs
          npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
